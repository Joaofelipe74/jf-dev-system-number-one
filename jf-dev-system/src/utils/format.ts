/** Re-exporta formatadores usados na camada de apresentação (mantém `utils/` coeso com o briefing do projeto). */
export { formatCurrency, formatDuration, getInitials, maskPhone, onlyDigits } from '@/lib/utils';
